application {
    name = "downloadAnalyticsGagdet"
}
environments {
    development {
        server {
            url = "http://localhost:8080"
            repository = "repository"
            workspace = "wsrp-system"
            login = "root" //this should probably put somewhere else
            password = "gtn" //this should probably put somewhere else
            autoload = true
        }
    }
}